---
interface Props {
  word: string;
  slug: string;
}
const { word, slug } = Astro.props;
---

<a
  href={`/glossary/${slug}`}
  class="text-[var(--c-gold)] underline underline-offset-2 decoration-dotted hover:text-[var(--c-gold-light)] transition-colors duration-150"
>{word}</a>
